-- Executor Check
local supportedExecutors = {"Volt", "Wave"}
local executorName = (identifyexecutor and identifyexecutor()) or "Unknown"

local isSupported = false
for _, name in ipairs(supportedExecutors) do
    if string.lower(executorName):find(string.lower(name)) then
        isSupported = true
        break
    end
end

-- GUI Setup
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local ScreenGui = Instance.new("ScreenGui")
local Blur = Instance.new("Frame")
local Frame = Instance.new("Frame")
local UICorner = Instance.new("UICorner")
local TopBar = Instance.new("Frame")
local TopBarCorner = Instance.new("UICorner")
local Title = Instance.new("TextLabel")
local Divider = Instance.new("Frame")
local MessageLabel = Instance.new("TextLabel")
local Divider2 = Instance.new("Frame")
local DiscordSection = Instance.new("Frame")
local DiscordIcon = Instance.new("TextLabel")
local DiscordText = Instance.new("TextLabel")
local ButtonHolder = Instance.new("Frame")
local CloseButton = Instance.new("TextButton")
local CloseCorner = Instance.new("UICorner")
local DiscordButton = Instance.new("TextButton")
local DiscordCorner = Instance.new("UICorner")

ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false
ScreenGui.DisplayOrder = 999

-- Dark overlay
Blur.Size = UDim2.new(1, 0, 1, 0)
Blur.Position = UDim2.new(0, 0, 0, 0)
Blur.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Blur.BackgroundTransparency = 0.4
Blur.BorderSizePixel = 0
Blur.ZIndex = 1
Blur.Parent = ScreenGui

-- Main box
Frame.Size = UDim2.new(0, 340, 0, 270)
Frame.Position = UDim2.new(0.5, -170, 0.5, -135)
Frame.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
Frame.BorderSizePixel = 0
Frame.ZIndex = 2
Frame.Parent = ScreenGui

UICorner.CornerRadius = UDim.new(0, 12)
UICorner.Parent = Frame

-- Top bar
TopBar.Size = UDim2.new(1, 0, 0, 40)
TopBar.Position = UDim2.new(0, 0, 0, 0)
TopBar.BackgroundColor3 = isSupported and Color3.fromRGB(30, 120, 255) or Color3.fromRGB(200, 40, 40)
TopBar.BorderSizePixel = 0
TopBar.ZIndex = 3
TopBar.Parent = Frame

TopBarCorner.CornerRadius = UDim.new(0, 12)
TopBarCorner.Parent = TopBar

local TopBarFix = Instance.new("Frame")
TopBarFix.Size = UDim2.new(1, 0, 0.5, 0)
TopBarFix.Position = UDim2.new(0, 0, 0.5, 0)
TopBarFix.BackgroundColor3 = TopBar.BackgroundColor3
TopBarFix.BorderSizePixel = 0
TopBarFix.ZIndex = 3
TopBarFix.Parent = TopBar

-- Title
Title.Size = UDim2.new(1, 0, 1, 0)
Title.BackgroundTransparency = 1
Title.Text = isSupported and "✓  Kicia Hook Script" or "✗  Incompatible Executor"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextScaled = true
Title.Font = Enum.Font.GothamBold
Title.ZIndex = 4
Title.Parent = TopBar

-- Divider 1
Divider.Size = UDim2.new(0.9, 0, 0, 1)
Divider.Position = UDim2.new(0.05, 0, 0, 48)
Divider.BackgroundColor3 = Color3.fromRGB(50, 50, 65)
Divider.BorderSizePixel = 0
Divider.ZIndex = 3
Divider.Parent = Frame

-- Message (same TextSize as Discord text)
MessageLabel.Size = UDim2.new(0.85, 0, 0, 55)
MessageLabel.Position = UDim2.new(0.075, 0, 0, 55)
MessageLabel.BackgroundTransparency = 1
MessageLabel.TextColor3 = Color3.fromRGB(210, 210, 220)
MessageLabel.TextScaled = false
MessageLabel.TextSize = 16
MessageLabel.Font = Enum.Font.Gotham
MessageLabel.TextWrapped = true
MessageLabel.ZIndex = 3
MessageLabel.Parent = Frame

if isSupported then
    MessageLabel.Text = "Hi! Welcome to Kicia Hook Script.\nRunning on: " .. executorName
else
    MessageLabel.Text = "Your executor is not supported.\nOnly Volt and Wave work with this script."
    MessageLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
end

-- Divider 2
Divider2.Size = UDim2.new(0.9, 0, 0, 1)
Divider2.Position = UDim2.new(0.05, 0, 0, 118)
Divider2.BackgroundColor3 = Color3.fromRGB(50, 50, 65)
Divider2.BorderSizePixel = 0
Divider2.ZIndex = 3
Divider2.Parent = Frame

-- Discord Section background
DiscordSection.Size = UDim2.new(0.9, 0, 0, 70)
DiscordSection.Position = UDim2.new(0.05, 0, 0, 126)
DiscordSection.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
DiscordSection.BorderSizePixel = 0
DiscordSection.ZIndex = 3
DiscordSection.Parent = Frame

local DiscordSectionCorner = Instance.new("UICorner")
DiscordSectionCorner.CornerRadius = UDim.new(0, 8)
DiscordSectionCorner.Parent = DiscordSection

-- Discord icon
DiscordIcon.Size = UDim2.new(0, 30, 0, 30)
DiscordIcon.Position = UDim2.new(0, 10, 0.5, -15)
DiscordIcon.BackgroundTransparency = 1
DiscordIcon.Text = "💬"
DiscordIcon.TextScaled = true
DiscordIcon.ZIndex = 4
DiscordIcon.Parent = DiscordSection

-- Discord promo text (same TextSize as message)
DiscordText.Size = UDim2.new(1, -50, 1, 0)
DiscordText.Position = UDim2.new(0, 45, 0, 0)
DiscordText.BackgroundTransparency = 1
DiscordText.Text = "Join our Discord for more scripts,\nupdates & support!"
DiscordText.TextColor3 = Color3.fromRGB(180, 185, 255)
DiscordText.TextScaled = false
DiscordText.TextSize = 16
DiscordText.Font = Enum.Font.Gotham
DiscordText.TextWrapped = true
DiscordText.TextXAlignment = Enum.TextXAlignment.Left
DiscordText.ZIndex = 4
DiscordText.Parent = DiscordSection

-- Button holder
ButtonHolder.Size = UDim2.new(0.9, 0, 0, 38)
ButtonHolder.Position = UDim2.new(0.05, 0, 1, -50)
ButtonHolder.BackgroundTransparency = 1
ButtonHolder.ZIndex = 3
ButtonHolder.Parent = Frame

local ButtonLayout = Instance.new("UIListLayout")
ButtonLayout.FillDirection = Enum.FillDirection.Horizontal
ButtonLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
ButtonLayout.VerticalAlignment = Enum.VerticalAlignment.Center
ButtonLayout.Padding = UDim.new(0, 10)
ButtonLayout.Parent = ButtonHolder

-- Close button
CloseButton.Size = UDim2.new(0, 140, 0, 34)
CloseButton.BackgroundColor3 = isSupported and Color3.fromRGB(30, 120, 255) or Color3.fromRGB(200, 40, 40)
CloseButton.Text = "Close"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.TextScaled = true
CloseButton.BorderSizePixel = 0
CloseButton.ZIndex = 3
CloseButton.Parent = ButtonHolder

CloseCorner.CornerRadius = UDim.new(0, 8)
CloseCorner.Parent = CloseButton

-- Discord button
DiscordButton.Size = UDim2.new(0, 140, 0, 34)
DiscordButton.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
DiscordButton.Text = "Join Discord"
DiscordButton.TextColor3 = Color3.fromRGB(255, 255, 255)
DiscordButton.Font = Enum.Font.GothamBold
DiscordButton.TextScaled = true
DiscordButton.BorderSizePixel = 0
DiscordButton.ZIndex = 3
DiscordButton.Parent = ButtonHolder

DiscordCorner.CornerRadius = UDim.new(0, 8)
DiscordCorner.Parent = DiscordButton

-- Button actions
CloseButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

DiscordButton.MouseButton1Click:Connect(function()
    setclipboard("https://discord.gg/YOURINVITEHERE")

    local hint = Instance.new("TextLabel")
    hint.Size = UDim2.new(0.8, 0, 0, 22)
    hint.Position = UDim2.new(0.1, 0, 1, -78)
    hint.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    hint.TextColor3 = Color3.fromRGB(150, 255, 150)
    hint.Text = "✓ Discord link copied to clipboard!"
    hint.Font = Enum.Font.Gotham
    hint.TextScaled = true
    hint.BorderSizePixel = 0
    hint.ZIndex = 5
    hint.Parent = Frame

    local hintCorner = Instance.new("UICorner")
    hintCorner.CornerRadius = UDim.new(0, 6)
    hintCorner.Parent = hint

    task.delay(2, function()
        hint:Destroy()
    end)
end)

-- Stop script if executor not supported
if not isSupported then return end

-- Your actual script continues below this line...
